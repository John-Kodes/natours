/* eslint-disable */

// This will run client side
const locations = JSON.parse(document.getElementById('map').dataset.locations); // getting access to the dataset and pqarsing it to a JSON
// console.log(locations);
